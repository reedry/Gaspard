export type StateName = "Menu" | "Flashcard" | "Result";

export type ColumnNumbers = {
  entry: number;
  check: number;
  categoryFrom: number;
  categoryTo: number;
  front: number;
  back: number;
  notes: number;
};
